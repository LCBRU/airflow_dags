{% if cursor | length > 0 %}
    <h2>Missing Study CiviCRM Contacts</h2>
    <p>
        The following CiviCRM contacts 
        are missing from the Study Data Warehouses
        that are defined in etl__civicrm_mapping
    </p>
    <ul>
        {% for r in cursor %}
            <li>
                <dl>
                    <dt>Study</dt>
                    <dd>{{ r['study_database'] }}</dd>
                    <dt>Case Type ID</dt>
                    <dd>{{ r['case_type_id'] }}</dd>
                    <dt>Contact ID</dt>
                    <dd>{{ r['contact_id'] }}</dd>
                </dl>
            </li>
        {% endfor %}
    </ul>
{% endif %}
